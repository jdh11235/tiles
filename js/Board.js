function Board () {
	//integrate UI updating into Tile
}

Board.prototype.tiles = [];

Board.prototype.init = function () {
//	for x for y this.tiles[x][y] = new Tile()
};

Board.prototype.createTile = function (where) { // {x, y}
};

Board.prototype.moveTile = function (from, to) { //{x, y}, {x, y}

};
